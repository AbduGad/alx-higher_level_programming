#include <stdio.h>

struct GenericNode {
    void* data; // Pointer to data of unknown type
    struct GenericNode* next;
};

typedef struct {
int i;
list_i *next;
}list_i;

int main()
{
	// list_i *node;
	// list_i (char *)node->i = "str";
	// return 0;
}